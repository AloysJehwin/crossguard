{
  "name": "crossguard-contracts",
  "version": "1.0.0",
  "description": "CrossGuard Insurance Smart Contracts for Somnia",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:somnia": "hardhat run scripts/deploy-somnia.js --network somnia-testnet",
    "verify": "hardhat verify --network somnia-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.26.3"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
